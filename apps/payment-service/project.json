{
  "name": "payment-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/payment-service/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["dist/apps/payment-service"],
      "options": { "command": "npx tsc -p apps/payment-service/tsconfig.json --rootDir ." },
      "cache": true
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "node dist/apps/payment-service/apps/payment-service/src/main.js" }
    }
  }
}
