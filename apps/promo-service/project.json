{
  "name": "promo-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/promo-service/src",
  "projectType": "application",
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "npx tsc -p apps/promo-service/tsconfig.json --rootDir ." } },
    "serve": { "executor": "nx:run-commands", "options": { "command": "./node_modules/.bin/ts-node --transpile-only -r tsconfig-paths/register -P apps/promo-service/tsconfig.json apps/promo-service/src/main.ts", "cwd": "." } }
  }
}
