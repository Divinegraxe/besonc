{
  "name": "vendor-bff",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/vendor-bff/src",
  "projectType": "application",
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "npx tsc -p apps/vendor-bff/tsconfig.json --rootDir ." } },
    "serve": { "executor": "nx:run-commands", "options": { "command": "./node_modules/.bin/ts-node --transpile-only -r tsconfig-paths/register -P apps/vendor-bff/tsconfig.json apps/vendor-bff/src/main.ts", "cwd": "." } }
  }
}
